@model HealthSolutions_MVC.Models.TipoSexo

@{
    ViewData["Title"] = "Tipo Sexo";
}

<h1>Detalhes</h1>

<div>
    <h4>Sexo</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.NomeTipoSexo)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.NomeTipoSexo)
        </dd>
    </dl>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model?.TipoSexoId" class="btn btn-primary">Editar</a> 
    <a asp-action="Index" class="btn btn-primary">Voltar à  Lista</a>
</div>
