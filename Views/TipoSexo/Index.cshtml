@model IEnumerable<HealthSolutions_MVC.Models.TipoSexo>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NomeTipoSexo)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NomeTipoSexo)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.TipoSexoId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.TipoSexoId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.TipoSexoId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
